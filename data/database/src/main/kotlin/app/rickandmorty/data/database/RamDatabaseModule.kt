package app.rickandmorty.data.database

private const val DATABASE_NAME = "ram-database.db"

// internal object RamDatabaseModule {
//  fun provideAppDatabase(context: Context): RamDatabase =
//    Room.databaseBuilder<RamDatabase>(context = context, name = DATABASE_NAME).build()
// }
