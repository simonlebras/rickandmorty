query GetLocations($page: Int!) {
    locations(page: $page) {
        info {
            count
            next
        }

        results {
            id
            name
            type
            dimension
        }
    }
}
