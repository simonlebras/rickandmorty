query GetCharacters($page: Int!) {
    characters(page: $page) {
        info {
            count
            next
        }

        results {
            id
            name
            status
            species
            type
            gender
            image
        }
    }
}
